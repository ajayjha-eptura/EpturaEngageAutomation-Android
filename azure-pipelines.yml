# Azure Pipeline for Eptura Engage Android Automation
# This pipeline creates an emulator with Google Play Store and installs app from Play Store

trigger:
  branches:
    include:
      - main
      - master
      - develop
  paths:
    exclude:
      - README.md
      - docs/**

pr:
  branches:
    include:
      - main
      - master
      - develop

# Pipeline variables
variables:
  mavenPOMFile: 'pom.xml'
  jdkVersion: '17'
  appiumVersion: '3.0.0'
  androidSdkVersion: '30'
  testResultsFolder: '$(System.DefaultWorkingDirectory)/target/surefire-reports'
  screenshotsFolder: '$(System.DefaultWorkingDirectory)/target/screenshots'
  extentReportFolder: '$(System.DefaultWorkingDirectory)/target'
  emulatorName: 'test_emulator_playstore'

# Agent pool - Use Microsoft-hosted macOS agents
pool:
  vmImage: 'macOS-14'

stages:
  - stage: Build
    displayName: 'Build Stage'
    jobs:
      - job: MavenBuild
        displayName: 'Build Maven Project'
        steps:
          - task: JavaToolInstaller@0
            displayName: 'Install Java JDK 17'
            inputs:
              versionSpec: '17'
              jdkArchitectureOption: 'x64'
              jdkSourceOption: 'PreInstalled'

          - task: Maven@4
            displayName: 'Maven Clean Compile'
            inputs:
              mavenPomFile: '$(mavenPOMFile)'
              goals: 'clean compile'
              options: '-DskipTests'
              publishJUnitResults: false
              javaHomeOption: 'JDKVersion'
              jdkVersionOption: '$(jdkVersion)'
              mavenVersionOption: 'Default'
              mavenOptions: '-Xmx3072m'

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Build Artifacts'
            inputs:
              PathtoPublish: '$(System.DefaultWorkingDirectory)/target'
              ArtifactName: 'build-output'
              publishLocation: 'Container'

  - stage: Test
    displayName: 'Test Stage'
    dependsOn: Build
    jobs:
      - job: AndroidTests
        displayName: 'Run Android Appium Tests'
        timeoutInMinutes: 120
        steps:
          - task: JavaToolInstaller@0
            displayName: 'Install Java JDK 17'
            inputs:
              versionSpec: '17'
              jdkArchitectureOption: 'x64'
              jdkSourceOption: 'PreInstalled'

          - task: NodeTool@0
            displayName: 'Install Node.js'
            inputs:
              versionSpec: '20.x'

          - bash: |
              echo "🐍 Installing Python dependencies for Play Store installer..."
              python3 --version
              pip3 --version
              
              # Install Appium Python client and dependencies
              # Using --break-system-packages flag to bypass PEP 668 restrictions on macOS
              # Allow pip upgrade to fail as it's not critical
              pip3 install --upgrade pip --break-system-packages || echo "⚠️  Pip upgrade skipped (not critical)"
              
              # Install required packages
              pip3 install Appium-Python-Client --break-system-packages || { echo "❌ Failed to install Appium-Python-Client"; exit 1; }
              pip3 install selenium --break-system-packages || { echo "❌ Failed to install selenium"; exit 1; }
              
              echo "✅ Python dependencies installed"
              pip3 list | grep -i appium || echo "Appium package verification"
            displayName: 'Install Python Dependencies'

          - bash: |
              echo "📱 Configuring Android SDK with Google Play support..."
              
              # Set Android environment variables
              export ANDROID_HOME=$ANDROID_SDK_ROOT
              export PATH=$PATH:$ANDROID_HOME/platform-tools:$ANDROID_HOME/emulator:$ANDROID_HOME/cmdline-tools/latest/bin
              
              echo "ANDROID_HOME: $ANDROID_HOME"
              echo "ANDROID_SDK_ROOT: $ANDROID_SDK_ROOT"
              
              # Verify Android SDK exists
              if [ ! -d "$ANDROID_HOME" ]; then
                echo "❌ ANDROID_HOME directory not found: $ANDROID_HOME"
                exit 1
              fi
              
              echo "📋 Android SDK structure:"
              ls -la $ANDROID_HOME/ || true
              
              # Verify cmdline-tools exists
              if [ ! -d "$ANDROID_HOME/cmdline-tools/latest/bin" ]; then
                echo "❌ cmdline-tools not found. Checking for alternative locations..."
                find $ANDROID_HOME -name "sdkmanager" -type f 2>/dev/null || true
                exit 1
              fi
              
              # Accept all licenses - use a more robust method
              echo "📝 Accepting Android SDK licenses..."
              # Create licenses directory if it doesn't exist
              mkdir -p "$ANDROID_HOME/licenses"
              
              # Accept all known Android SDK licenses by creating license files
              # These are the current license hashes as of 2024
              echo "24333f8a63b6825ea9c5514f83c2829b004d1fee" > "$ANDROID_HOME/licenses/android-sdk-license"
              echo "84831b9409646a918e30573bab4c9c91346d8abd" > "$ANDROID_HOME/licenses/android-sdk-preview-license"
              echo "d975f751698a77b662f1254ddbeed3901e976f5a" > "$ANDROID_HOME/licenses/intel-android-extra-license"
              echo "33b6a2b64607f11b759f320ef9dff4ae5c47d97a" > "$ANDROID_HOME/licenses/google-gdk-license"
              echo "e9acab5b5fbb560a72cfaecce8946896ff6aab9d" > "$ANDROID_HOME/licenses/mips-android-sysimage-license"
              echo "d56a98520377fecf01c9e79e689e9ba188a4558e" > "$ANDROID_HOME/licenses/android-googletv-license"
              echo "667f5ef1d0d6dbb3c1bb1dbe94f0c0c66f8c3ee5" > "$ANDROID_HOME/licenses/android-googlexr-license"
              
              # Accept the ARM DBT license with all known hashes
              echo "e6b7c2ab7fa2298c15165e9583d0acf0b04a2232" > "$ANDROID_HOME/licenses/android-sdk-arm-dbt-license"
              echo "d56a98520377fecf01c9e79e689e9ba188a4558e" >> "$ANDROID_HOME/licenses/android-sdk-arm-dbt-license"
              
              echo "✅ License files created successfully"
              
              # Verify licenses directory
              echo "📋 Licenses directory contents:"
              ls -la "$ANDROID_HOME/licenses/" || echo "Licenses directory not found"
              cat "$ANDROID_HOME/licenses/android-sdk-arm-dbt-license" 2>/dev/null && echo "ARM DBT license hashes verified"
              
              # Update SDK manager
              echo "🔄 Updating SDK manager..."
              $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --update --sdk_root=$ANDROID_HOME || true
              
              # Install SDK components with Google Play Store support
              # Use 'yes' to automatically accept any interactive prompts
              echo "📦 Installing SDK components with Google Play Store..."
              echo "This may take a few minutes..."
              
              yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager \
                --sdk_root=$ANDROID_HOME \
                --verbose \
                "platform-tools" \
                "platforms;android-30" \
                "build-tools;30.0.3" \
                "emulator" \
                "system-images;android-30;google_apis_playstore;x86_64" 2>&1 | grep -v "^Info:" | grep -v "^Warning:" || {
                echo "❌ Failed to install SDK components"
                echo ""
                echo "Checking what went wrong..."
                echo "Attempting to list available packages:"
                $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --list --sdk_root=$ANDROID_HOME 2>&1 | head -50 || true
                echo ""
                echo "Checking licenses directory:"
                ls -la "$ANDROID_HOME/licenses/"
                exit 1
              }
              
              echo "✅ SDK components installation completed"
              
              # Wait for installation to complete and verify
              sleep 5
              
              # Verify system image was installed
              echo "🔍 Verifying system image installation..."
              echo "Checking path: $ANDROID_HOME/system-images/android-30/google_apis_playstore/x86_64"
              
              if [ -d "$ANDROID_HOME/system-images/android-30/google_apis_playstore/x86_64" ]; then
                echo "✅ Google Play system image found!"
                ls -la "$ANDROID_HOME/system-images/android-30/google_apis_playstore/x86_64"
              else
                echo "❌ System image not found after installation!"
                echo ""
                echo "Contents of system-images directory:"
                if [ -d "$ANDROID_HOME/system-images" ]; then
                  find $ANDROID_HOME/system-images -type d -maxdepth 3
                else
                  echo "⚠️  system-images directory does not exist at: $ANDROID_HOME/system-images"
                fi
                echo ""
                echo "📋 List of installed packages:"
                $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --list_installed --sdk_root=$ANDROID_HOME
                exit 1
              fi
              
              # Verify ADB
              echo "🔧 Verifying ADB..."
              adb version
              
              # List all installed system images
              echo "📋 Installed system images:"
              $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --list_installed --sdk_root=$ANDROID_HOME | grep system-images || echo "No system images found in installed list"
              
              echo ""
              echo "📱 Creating Android Emulator with Google Play Store..."
              
              # Delete existing emulator if present
              $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager delete avd -n $(emulatorName) 2>/dev/null || true
              
              # Create new emulator with Google Play
              echo "Creating emulator with Google Play Store support..."
              echo "no" | $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd \
                --force \
                --name "$(emulatorName)" \
                --package "system-images;android-30;google_apis_playstore;x86_64" \
                --device "pixel_4" \
                --abi x86_64 || {
                echo "❌ Failed to create emulator"
                echo "Available packages:"
                $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager list target
                exit 1
              }
              
              # Verify emulator was created
              echo "🔍 Verifying emulator creation..."
              $ANDROID_HOME/emulator/emulator -list-avds
              
              if $ANDROID_HOME/emulator/emulator -list-avds | grep -q "$(emulatorName)"; then
                echo "✅ Emulator '$(emulatorName)' created successfully"
              else
                echo "❌ Failed to create emulator"
                echo "Available AVDs:"
                $ANDROID_HOME/emulator/emulator -list-avds
                exit 1
              fi
              
              # Start emulator with optimized settings
              echo "🚀 Starting emulator with optimized settings..."
              nohup $ANDROID_HOME/emulator/emulator -avd $(emulatorName) \
                -no-snapshot-save \
                -no-window \
                -no-audio \
                -no-boot-anim \
                -gpu swiftshader_indirect \
                -memory 4096 \
                -cores 2 \
                -accel on \
                -wipe-data \
                > emulator.log 2>&1 &
              
              EMULATOR_PID=$!
              echo "Emulator PID: $EMULATOR_PID"
              echo "##vso[task.setvariable variable=EMULATOR_PID]$EMULATOR_PID"
              
              # Wait for device to be detected
              echo "⏳ Waiting for emulator device to be detected..."
              adb wait-for-device
              echo "✅ Device detected"
              
              # Wait for boot completion with reasonable timeout
              echo "⏳ Waiting for boot_completed flag..."
              timeout=180
              elapsed=0
              until [[ "$(adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')" == "1" ]]; do
                if [ $elapsed -ge $timeout ]; then
                  echo "❌ Boot timeout after $timeout seconds"
                  echo "=== Emulator Log (last 100 lines) ==="
                  tail -100 emulator.log
                  exit 1
                fi
                sleep 5
                elapsed=$((elapsed+5))
                if [ $((elapsed % 15)) -eq 0 ]; then
                  echo "  Still booting... ($elapsed/$timeout seconds)"
                fi
              done
              
              echo "✅ Boot completed flag set!"
              
              # Optimized service readiness check with faster polling
              echo "⏳ Waiting for Android system services (max 90 seconds)..."
              
              service_ready=false
              max_wait=90
              elapsed=0
              
              while [ $elapsed -lt $max_wait ]; do
                # Check all services in one go
                pm_ready=false
                am_ready=false
                
                if adb shell pm list packages >/dev/null 2>&1; then
                  pm_ready=true
                fi
                
                if adb shell am get-config >/dev/null 2>&1; then
                  am_ready=true
                fi
                
                if [ "$pm_ready" = true ] && [ "$am_ready" = true ]; then
                  echo "✅ All critical services are ready!"
                  service_ready=true
                  break
                fi
                
                sleep 3
                elapsed=$((elapsed+3))
                
                if [ $((elapsed % 15)) -eq 0 ]; then
                  echo "  Still initializing services... ($elapsed/$max_wait seconds)"
                  echo "    - Package Manager: $([ "$pm_ready" = true ] && echo "✅" || echo "⏳")"
                  echo "    - Activity Manager: $([ "$am_ready" = true ] && echo "✅" || echo "⏳")"
                fi
              done
              
              if [ "$service_ready" = false ]; then
                echo "⚠️  Services not ready after $max_wait seconds, but continuing..."
                echo "Attempting to check emulator state:"
                adb shell getprop sys.boot_completed
                adb shell pm list packages | head -5
              fi
              
              # Extended stability wait for emulator to fully initialize
              echo "⏳ Allowing system to stabilize (30 seconds)..."
              sleep 30
              
              # Final verification
              echo "🔍 Final system verification..."
              
              # Verify ADB connection is stable with extended retries
              echo "⏳ Verifying ADB shell connection..."
              adb_shell_ready=false
              for attempt in {1..10}; do
                echo "  Attempt $attempt/10: Testing ADB shell..."
                if adb shell echo "test" > /dev/null 2>&1; then
                  echo "✅ ADB shell connection established (attempt $attempt)"
                  adb_shell_ready=true
                  break
                fi
                echo "  ⏳ ADB shell not ready, waiting 15 seconds..."
                sleep 15
              done
              
              if [ "$adb_shell_ready" = false ]; then
                echo "⚠️  ADB shell not responding after 150 seconds"
                echo "Checking emulator status:"
                adb devices -l
                echo ""
                echo "Checking emulator process:"
                ps aux | grep emulator | grep -v grep || echo "Emulator process not found"
                echo ""
                echo "Attempting to restart ADB..."
                adb kill-server
                sleep 3
                adb start-server
                sleep 5
                adb wait-for-device
                sleep 10
                
                # Extended retry after ADB restart
                echo "Retrying ADB shell connection (5 more attempts)..."
                for attempt in {1..5}; do
                  echo "  Post-restart attempt $attempt/5..."
                  if adb shell echo "test" > /dev/null 2>&1; then
                    echo "✅ ADB shell connection recovered after restart (attempt $attempt)"
                    adb_shell_ready=true
                    break
                  fi
                  sleep 15
                done
                
                if [ "$adb_shell_ready" = false ]; then
                  echo "❌ ADB shell connection failed after all retries"
                  echo ""
                  echo "=== Emulator Log (last 100 lines) ==="
                  tail -100 emulator.log || echo "Could not read emulator log"
                  echo ""
                  echo "=== ADB Devices ==="
                  adb devices -l
                  echo ""
                  echo "⚠️  Continuing anyway - tests may fail if emulator is not responsive"
                  # Don't exit - allow the pipeline to continue and let the test phase handle failures
                fi
              fi
              
              # Additional wait for stability after ADB connection verified
              if [ "$adb_shell_ready" = true ]; then
                echo "⏳ Additional stabilization wait (10 seconds)..."
                sleep 10
              fi
              
              # Verify package manager with retry
              echo "🔍 Verifying package manager..."
              pm_verified=false
              for i in {1..5}; do
                echo "  Attempt $i/5: Checking package manager..."
                if adb shell pm list packages > /dev/null 2>&1; then
                  pm_verified=true
                  echo "✅ Package manager verified"
                  break
                fi
                echo "  Retry $i/5 for package manager..."
                sleep 10
              done
              
              if [ "$pm_verified" = false ]; then
                echo "⚠️  Package manager not responding reliably"
                echo "Checking system_server process:"
                adb shell ps | grep system_server || echo "Could not check system_server"
                echo "⚠️  Continuing anyway - tests may fail if package manager is not ready"
              fi
              
              # Verify emulator is ready
              echo ""
              echo "📱 Emulator status:"
              adb devices -l
              
              # Try to get Android version with retry
              android_version="unknown"
              for i in {1..3}; do
                android_version=$(adb shell getprop ro.build.version.release 2>/dev/null | tr -d '\r\n')
                if [ -n "$android_version" ]; then
                  break
                fi
                sleep 3
              done
              echo "Android version: $android_version"
              
              # Try to get API level with retry
              api_level="unknown"
              for i in {1..3}; do
                api_level=$(adb shell getprop ro.build.version.sdk 2>/dev/null | tr -d '\r\n')
                if [ -n "$api_level" ]; then
                  break
                fi
                sleep 3
              done
              echo "API Level: $api_level"
              
              # Check for Google Play Store
              echo ""
              echo "🔍 Checking for Google Play Store..."
              play_store_found=false
              for i in {1..3}; do
                if adb shell pm list packages 2>/dev/null | grep -q "com.android.vending"; then
                  play_store_found=true
                  echo "✅ Google Play Store is present!"
                  PLAY_STORE_VERSION=$(adb shell dumpsys package com.android.vending 2>/dev/null | grep versionName | head -1 | awk '{print $1}' || echo "version unknown")
                  echo "Play Store: $PLAY_STORE_VERSION"
                  break
                fi
                echo "  Attempt $i/3: Play Store not detected yet, waiting..."
                sleep 10
              done
              
              if [ "$play_store_found" = false ]; then
                echo "⚠️  Google Play Store not found after multiple checks"
                echo "📋 Checking Google services packages:"
                adb shell pm list packages 2>/dev/null | grep -i google | head -10 || echo "Could not list packages"
              fi
              
              # Get emulator serial
              EMULATOR_SERIAL=$(adb devices | grep "emulator" | head -1 | awk '{print $1}')
              echo ""
              echo "Emulator Serial: $EMULATOR_SERIAL"
              echo "##vso[task.setvariable variable=EMULATOR_SERIAL]$EMULATOR_SERIAL"
              
              echo ""
              echo "✅ Android emulator setup complete!"
              echo "Note: If emulator is not fully responsive, tests may fail. Check emulator logs for details."
            displayName: 'Setup Android SDK and Create Emulator with Google Play'

          - task: Npm@1
            displayName: 'Install Appium'
            inputs:
              command: 'custom'
              customCommand: 'install -g appium@$(appiumVersion)'

          - bash: |
              echo "🔌 Installing Appium UiAutomator2 Driver..."
              appium driver install uiautomator2
              appium driver list --installed
              echo "✅ Driver installed"
            displayName: 'Install Appium Driver'

          - bash: |
              echo "🚀 Starting Appium Server..."
              mkdir -p logs
              
              # Clean up any existing Appium processes
              pkill -f appium || true
              sleep 2
              
              # Start Appium
              nohup appium --address 127.0.0.1 --port 4723 --session-override --log-level info > logs/appium.log 2>&1 &
              APPIUM_PID=$!
              echo "Appium PID: $APPIUM_PID"
              echo "##vso[task.setvariable variable=APPIUM_PID]$APPIUM_PID"
              
              # Wait for Appium to be ready
              for i in {1..30}; do
                if curl -s http://127.0.0.1:4723/status > /dev/null 2>&1; then
                  echo "✅ Appium server is ready"
                  exit 0
                fi
                echo "Waiting... ($i/30)"
                sleep 2
              done
              
              echo "❌ Appium failed to start"
              cat logs/appium.log
              exit 1
            displayName: 'Start Appium Server'

          - bash: |
              echo "📲 Installing Eptura Engage App..."
              echo "======================================================="
              
              # Define package name
              PACKAGE_NAME="com.condecosoftware.condeco"
              
              # Check if app is already installed
              if adb shell pm list packages | grep -q "$PACKAGE_NAME"; then
                echo "✅ App is already installed!"
                APP_INFO=$(adb shell dumpsys package $PACKAGE_NAME | grep -E "versionName|versionCode" | head -2)
                echo "App info:"
                echo "$APP_INFO"
                exit 0
              fi
              
              echo "📦 Method 1: Attempting direct APK installation..."
              echo ""
              
              # Check if APK file is provided in the repo
              if [ -f "app/Eptura_Engage.apk" ] || [ -f "Eptura_Engage.apk" ]; then
                APK_PATH=$(find . -name "Eptura_Engage.apk" -o -name "condeco.apk" | head -1)
                echo "✅ Found APK file: $APK_PATH"
                echo "📥 Installing APK..."
                
                adb install -r "$APK_PATH"
                
                if [ $? -eq 0 ]; then
                  echo "✅ APK installed successfully!"
                  exit 0
                else
                  echo "⚠️  APK installation failed, trying alternative methods..."
                fi
              else
                echo "ℹ️  No APK file found in repository"
              fi
              
              echo ""
              echo "📦 Method 2: Attempting Play Store installation with enhanced automation..."
              echo ""
              
              # Wake up the device and unlock
              echo "📱 Waking up device..."
              adb shell input keyevent KEYCODE_WAKEUP
              sleep 2
              adb shell input keyevent KEYCODE_MENU
              sleep 2
              
              # Try opening Play Store with deep link
              echo "🔗 Opening Play Store with deep link..."
              adb shell am start -a android.intent.action.VIEW -d "market://details?id=$PACKAGE_NAME"
              
              echo "⏳ Waiting for Play Store to load (20 seconds)..."
              sleep 20
              
              # Try to find and click Install button using multiple methods
              echo "🔍 Attempting to locate and click Install button..."
              
              # Method 1: Use uiautomator to find Install button coordinates
              echo "  Method 1: Using UI Automator to locate button..."
              adb shell uiautomator dump /sdcard/window_dump.xml 2>/dev/null
              sleep 2
              
              # Try to extract Install button coordinates from XML
              INSTALL_FOUND=false
              if adb pull /sdcard/window_dump.xml /tmp/window_dump.xml 2>/dev/null; then
                # Look for Install button in XML and extract bounds (BSD grep compatible)
                # Use extended regex instead of Perl regex
                BOUNDS=$(grep -i 'text="Install"' /tmp/window_dump.xml | grep -o 'bounds="\[[0-9]*,[0-9]*\]\[[0-9]*,[0-9]*\]"' | head -1 | sed 's/bounds="\[\([0-9]*\),\([0-9]*\)\]\[\([0-9]*\),\([0-9]*\)\]"/\1 \2 \3 \4/')
                
                if [ -z "$BOUNDS" ]; then
                  # Try INSTALL in caps
                  BOUNDS=$(grep -i 'text="INSTALL"' /tmp/window_dump.xml | grep -o 'bounds="\[[0-9]*,[0-9]*\]\[[0-9]*,[0-9]*\]"' | head -1 | sed 's/bounds="\[\([0-9]*\),\([0-9]*\)\]\[\([0-9]*\),\([0-9]*\)\]"/\1 \2 \3 \4/')
                fi
                
                if [ -n "$BOUNDS" ]; then
                  X1=$(echo $BOUNDS | awk '{print $1}')
                  Y1=$(echo $BOUNDS | awk '{print $2}')
                  X2=$(echo $BOUNDS | awk '{print $3}')
                  Y2=$(echo $BOUNDS | awk '{print $4}')
                  
                  # Calculate center of button
                  TAP_X=$(( (X1 + X2) / 2 ))
                  TAP_Y=$(( (Y1 + Y2) / 2 ))
                  
                  echo "  ✅ Found Install button at [$X1,$Y1][$X2,$Y2]"
                  echo "  🎯 Tapping at center: ($TAP_X, $TAP_Y)"
                  
                  adb shell input tap $TAP_X $TAP_Y
                  INSTALL_FOUND=true
                  echo "  ✅ Install button tapped!"
                  sleep 5
                else
                  echo "  ⚠️  Install button not found in UI dump"
                  echo "  Dumping XML content for debugging:"
                  grep -i "install\|play\|download" /tmp/window_dump.xml | head -20 || echo "No relevant content found"
                fi
              fi
              
              # Method 2: Try using accessibility service and key events
              if [ "$INSTALL_FOUND" = false ]; then
                echo ""
                echo "  Method 2: Using D-PAD navigation to find Install button..."
                
                # Move focus around the screen using D-PAD
                for i in {1..3}; do
                  adb shell input keyevent KEYCODE_DPAD_DOWN
                  sleep 1
                done
                
                # Try pressing enter
                echo "  Attempting to press ENTER on focused element..."
                adb shell input keyevent KEYCODE_ENTER
                sleep 3
                
                # Check if installation started
                adb shell uiautomator dump /sdcard/window_dump.xml 2>/dev/null
                if adb pull /sdcard/window_dump.xml /tmp/window_dump2.xml 2>/dev/null; then
                  if grep -qi "downloading\|installing\|pending" /tmp/window_dump2.xml; then
                    echo "  ✅ Installation appears to have started!"
                    INSTALL_FOUND=true
                  fi
                fi
              fi
              
              # Method 3: Try common button locations if UI Automator failed
              if [ "$INSTALL_FOUND" = false ]; then
                echo ""
                echo "  Method 3: Trying common Install button locations..."
                
                # Get screen dimensions
                SCREEN_SIZE=$(adb shell wm size | grep "Physical size" | cut -d: -f2 | tr -d ' ')
                WIDTH=$(echo $SCREEN_SIZE | cut -dx -f1)
                HEIGHT=$(echo $SCREEN_SIZE | cut -dx -f2)
                
                echo "  📱 Screen size: ${WIDTH}x${HEIGHT}"
                
                # Try multiple common locations for Install button (right side of screen, upper portion)
                declare -a TAP_LOCATIONS=(
                  "$((WIDTH * 4 / 5)) $((HEIGHT / 3))"      # Upper right
                  "$((WIDTH * 3 / 4)) $((HEIGHT / 3))"      # Upper right-center
                  "$((WIDTH * 7 / 8)) $((HEIGHT * 2 / 5))"  # Right side
                  "$((WIDTH * 5 / 6)) $((HEIGHT / 3))"      # Far right
                )
                
                for location in "${TAP_LOCATIONS[@]}"; do
                  X=$(echo $location | awk '{print $1}')
                  Y=$(echo $location | awk '{print $2}')
                  echo "  🎯 Trying tap at: ($X, $Y)"
                  adb shell input tap $X $Y
                  sleep 5
                  
                  # Check if installation started after this tap
                  adb shell uiautomator dump /sdcard/window_dump.xml 2>/dev/null
                  if adb pull /sdcard/window_dump.xml /tmp/window_dump3.xml 2>/dev/null; then
                    if grep -qi "downloading\|installing\|pending" /tmp/window_dump3.xml; then
                      echo "  ✅ Installation started after tap!"
                      INSTALL_FOUND=true
                      break
                    fi
                  fi
                done
              fi
              
              # Handle any confirmation dialogs
              echo ""
              echo "  Checking for confirmation dialogs..."
              sleep 3
              
              # Try to find and click Continue/Accept buttons
              adb shell uiautomator dump /sdcard/window_dump.xml 2>/dev/null
              if adb pull /sdcard/window_dump.xml /tmp/window_dump_dialogs.xml 2>/dev/null; then
                for BUTTON_TEXT in "Continue" "Accept" "OK" "Agree" "CONTINUE" "ACCEPT"; do
                  BOUNDS=$(grep "text=\"$BUTTON_TEXT\"" /tmp/window_dump_dialogs.xml | grep -o 'bounds="\[[0-9]*,[0-9]*\]\[[0-9]*,[0-9]*\]"' | head -1 | sed 's/bounds="\[\([0-9]*\),\([0-9]*\)\]\[\([0-9]*\),\([0-9]*\)\]"/\1 \2 \3 \4/')
                  if [ -n "$BOUNDS" ]; then
                    X1=$(echo $BOUNDS | awk '{print $1}')
                    Y1=$(echo $BOUNDS | awk '{print $2}')
                    X2=$(echo $BOUNDS | awk '{print $3}')
                    Y2=$(echo $BOUNDS | awk '{print $4}')
                    TAP_X=$(( (X1 + X2) / 2 ))
                    TAP_Y=$(( (Y1 + Y2) / 2 ))
                    echo "  ✅ Clicking '$BUTTON_TEXT' button at ($TAP_X, $TAP_Y)"
                    adb shell input tap $TAP_X $TAP_Y
                    sleep 3
                  fi
                done
              fi
              
              if [ "$INSTALL_FOUND" = false ]; then
                echo ""
                echo "⚠️  WARNING: Could not confirm Install button was clicked"
                echo "     Continuing to monitor for installation anyway..."
                echo ""
              fi
              
              echo ""
              echo "⏳ Waiting for installation to complete..."
              echo "   Monitoring: UI changes, download progress, and package manager"
              echo "   This may take 5-10 minutes for a 170 MB app"
              echo ""
              
              # Wait and check for installation with comprehensive monitoring
              MAX_WAIT=600  # 10 minutes for 170 MB app
              ELAPSED=0
              CHECK_INTERVAL=15
              LAST_PROGRESS=""
              
              while [ $ELAPSED -lt $MAX_WAIT ]; do
                # Check 1: Package manager (most reliable)
                if adb shell pm list packages | grep -q "$PACKAGE_NAME"; then
                  echo ""
                  echo "✅ App package detected in system!"
                  
                  # Verify it's fully installed (not just being installed)
                  sleep 10
                  
                  if adb shell pm list packages | grep -q "$PACKAGE_NAME"; then
                    echo "✅ Installation verified successfully!"
                    
                    # Get app info
                    APP_INFO=$(adb shell dumpsys package $PACKAGE_NAME | grep -E "versionName|versionCode" | head -2 2>/dev/null || echo "version info unavailable")
                    echo ""
                    echo "📱 App info:"
                    echo "$APP_INFO"
                    echo ""
                    exit 0
                  fi
                fi
                
                # Check 2: Look for progress indicators in UI
                if adb shell uiautomator dump /sdcard/window_dump.xml 2>/dev/null; then
                  if adb pull /sdcard/window_dump.xml /tmp/window_dump_monitor.xml 2>/dev/null; then
                    # Check for "Open" button (installation complete)
                    if grep -q 'text="Open"' /tmp/window_dump_monitor.xml || grep -q 'text="OPEN"' /tmp/window_dump_monitor.xml; then
                      echo ""
                      echo "✅ 'Open' button detected - installation complete!"
                      sleep 5
                      
                      # Verify in package manager
                      if adb shell pm list packages | grep -q "$PACKAGE_NAME"; then
                        echo "✅ Verified via package manager!"
                        exit 0
                      fi
                    fi
                    
                    # Check for download/install progress (BSD grep compatible - no -P flag)
                    # Look for percentage or status text
                    PROGRESS=$(grep -o 'text="[^"]*%' /tmp/window_dump_monitor.xml | head -1 | sed 's/text="//;s/%/%/')
                    if [ -z "$PROGRESS" ]; then
                      PROGRESS=$(grep -o 'text="Downloading[^"]*"' /tmp/window_dump_monitor.xml | head -1 | sed 's/text="//;s/"//')
                    fi
                    if [ -z "$PROGRESS" ]; then
                      PROGRESS=$(grep -o 'text="Installing[^"]*"' /tmp/window_dump_monitor.xml | head -1 | sed 's/text="//;s/"//')
                    fi
                    
                    if [ -n "$PROGRESS" ] && [ "$PROGRESS" != "$LAST_PROGRESS" ]; then
                      echo "  📥 Progress: $PROGRESS"
                      LAST_PROGRESS="$PROGRESS"
                    fi
                    
                    # Check for error messages
                    if grep -qE 'Error|Failed|Cannot|Unable' /tmp/window_dump_monitor.xml; then
                      ERROR_MSG=$(grep -o 'text="[^"]*Error[^"]*"' /tmp/window_dump_monitor.xml | head -1 | sed 's/text="//;s/"//')
                      if [ -z "$ERROR_MSG" ]; then
                        ERROR_MSG=$(grep -o 'text="[^"]*Failed[^"]*"' /tmp/window_dump_monitor.xml | head -1 | sed 's/text="//;s/"//')
                      fi
                      if [ -n "$ERROR_MSG" ]; then
                        echo "  ⚠️  Possible error: $ERROR_MSG"
                      fi
                    fi
                  fi
                fi
                
                sleep $CHECK_INTERVAL
                ELAPSED=$((ELAPSED + CHECK_INTERVAL))
                
                if [ $((ELAPSED % 60)) -eq 0 ]; then
                  MINUTES=$((ELAPSED / 60))
                  echo "  ⏳ Still waiting... ($MINUTES minutes elapsed)"
                  
                  # Show that we're still monitoring
                  if adb shell dumpsys window | grep -q "mCurrentFocus.*vending"; then
                    echo "     Play Store is still active"
                  fi
                fi
              done
              
              echo ""
              echo "⚠️  Installation timeout after $((MAX_WAIT / 60)) minutes"
              echo ""
              echo "💡 RECOMMENDATION: Add the APK file to your repository for faster, more reliable installation"
              echo "   Place the APK at: app/Eptura_Engage.apk or Eptura_Engage.apk"
              echo ""
              echo "📋 Current Play Store UI state (last dump):"
              if [ -f /tmp/window_dump_monitor.xml ]; then
                grep -i "install\|download\|error\|open" /tmp/window_dump_monitor.xml | head -30 || echo "No relevant UI elements found"
              fi
              echo ""
              echo "⚠️  Continuing pipeline - tests will likely fail without the app installed"
            displayName: 'Install App from Play Store'
            env:
              GOOGLE_PLAY_EMAIL: $(GOOGLE_PLAY_EMAIL)
              GOOGLE_PLAY_PASSWORD: $(GOOGLE_PLAY_PASSWORD)

          - task: DownloadBuildArtifacts@1
            displayName: 'Download Build Artifacts'
            inputs:
              buildType: 'current'
              downloadType: 'single'
              artifactName: 'build-output'
              downloadPath: '$(System.DefaultWorkingDirectory)'

          - task: Maven@4
            displayName: 'Run TestNG Tests'
            inputs:
              mavenPomFile: '$(mavenPOMFile)'
              goals: 'test'
              options: '-Dtestng.xml=testng.xml'
              publishJUnitResults: true
              testResultsFiles: '**/surefire-reports/TEST-*.xml'
              testRunTitle: 'Eptura Engage Android Tests'
              javaHomeOption: 'JDKVersion'
              jdkVersionOption: '$(jdkVersion)'
              mavenVersionOption: 'Default'
              mavenOptions: '-Xmx3072m'
            env:
              USE_EXTERNAL_APPIUM: 'true'
              APPIUM_SERVER_URL: 'http://127.0.0.1:4723'
            continueOnError: true

          - bash: |
              echo "🧹 Cleanup: Stopping Appium Server..."
              pkill -f appium || true
            displayName: 'Stop Appium Server'
            condition: always()

          - bash: |
              echo "🧹 Cleanup: Stopping Emulator..."
              adb emu kill || true
              pkill -f "emulator.*$(emulatorName)" || true
              sleep 5
            displayName: 'Stop Emulator'
            condition: always()

          - task: PublishTestResults@2
            displayName: 'Publish Test Results'
            inputs:
              testResultsFormat: 'JUnit'
              testResultsFiles: '**/surefire-reports/TEST-*.xml'
              mergeTestResults: true
              failTaskOnFailedTests: false
              testRunTitle: 'Eptura Android Tests'
            condition: always()

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Screenshots'
            inputs:
              PathtoPublish: '$(screenshotsFolder)'
              ArtifactName: 'test-screenshots'
            condition: always()

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Extent Report'
            inputs:
              PathtoPublish: '$(extentReportFolder)/Extent-report.html'
              ArtifactName: 'extent-report'
            condition: always()

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Logs'
            inputs:
              PathtoPublish: '$(System.DefaultWorkingDirectory)/logs'
              ArtifactName: 'appium-logs'
            condition: always()

          - bash: |
              if [ -f emulator.log ]; then
                mkdir -p logs
                cp emulator.log logs/
              fi
            displayName: 'Collect Emulator Logs'
            condition: always()

          - task: PublishBuildArtifacts@1
            displayName: 'Publish Emulator Logs'
            inputs:
              PathtoPublish: '$(System.DefaultWorkingDirectory)/logs'
              ArtifactName: 'emulator-logs'
            condition: always()

  - stage: Report
    displayName: 'Test Reports'
    dependsOn: Test
    condition: always()
    jobs:
      - job: Summary
        displayName: 'Test Summary'
        steps:
          - task: DownloadBuildArtifacts@1
            inputs:
              buildType: 'current'
              downloadType: 'specific'
              itemPattern: '**'
              downloadPath: '$(System.DefaultWorkingDirectory)/artifacts'

          - bash: |
              echo "📊 Test Execution Summary"
              echo "=========================="
              if [ -d "artifacts/test-screenshots" ]; then
                SCREENSHOT_COUNT=$(find artifacts/test-screenshots -type f -name "*.png" | wc -l)
                echo "📸 Screenshots: $SCREENSHOT_COUNT"
              fi
              
              if [ -f "artifacts/extent-report/Extent-report.html" ]; then
                echo "✅ Extent Report: Available"
              fi
              
              echo ""
              echo "View artifacts in the 'Summary' tab of this pipeline run"
            displayName: 'Display Summary'
